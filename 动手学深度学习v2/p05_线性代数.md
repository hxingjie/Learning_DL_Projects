```python
# 线性代数

import torch

# 1.标量
x = torch.tensor([2.0])
y = torch.tensor([3.0])
# x + y, x - y, x * y, x / y, x ** y

# 2.向量
x = torch.arange(4)
x = torch.tensor([0,1,2,3])
print(x[2]) # 下标访问

```
